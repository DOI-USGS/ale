Chandrayaan2 OHRC Instrument Addendum Kernel
=============================================================

This instrument addendum kernel (I-kernel) contains parameters that
describe the Chandrayaan-2 Orbiter High-Resolution Camera (OHRC) instrument.

\begintext
The following are the optical distortion parameters.
These are used to transform from observed (distorted)
coordinates (unprimed, e.g., x) to ideal coordinates
(primed, e.g., xp). Both sets of coordinates are expressed
in millimeters.

r = sqrt(x^2 + y^2)
dr = k0 * r + k1 * r^3 + k2 * r^5
rp = r - dr
xp = x * (rp/r), similarly for yp

or, rearranging a bit, we have a more efficient version:

r^2 = x^2 + y^2
dr/r = k0 + r^2 * (k1 + r^2 * k2)
xp = x - (dr/r) * x
yp = y - (dr/r) * y

Note that these distortion coefficients are currently placeholders and
will need to be updated with actual calibration data for the OHRC instrument.

\begindata
INS-152270_OD_K = ( 0.0, 0.0, 0.0 )

\begintext
The following are the parameters for computing focal plane
coords from CCD coords.

x = transx[0] + transx[1] * ccdSample_c + transx[2] * ccdLine_c
y = transy[0] + transy[1] * ccdSample_c + transy[2] * ccdLine_c

Note: The specific values for these transformation matrices should be
derived from OHRC's instrument calibration. The values provided here
are illustrative placeholders.

\begindata
INS-152270_TRANSX=( 0.0, 0.0, 0.0052 )
INS-152270_TRANSY=( 0.0, 0.0052, 0.0 )

\begintext
Parameters for computing CCD coords from focal plane coords

\begindata
INS-152270_ITRANSS=( 0.0, 0.0, 192.30769231 )
INS-152270_ITRANSL=( 0.0, 192.30769231, 0.0 )

\begintext
The following accounts for the rotation of the Chandrayaan-2 OHRC camera
for alignment with the pointing direction.

The number -152280 is set in the ISIS code as -152270 - 10.

\begindata

FRAME_CH2_OHRC_ISIS              =  -152280
FRAME_-152280_NAME               = 'CH2_OHRC_ISIS'
FRAME_-152280_CLASS              =  4
FRAME_-152280_CLASS_ID           =  -152280
FRAME_-152280_CENTER             =  -152
TKFRAME_-152280_RELATIVE         = 'CH2_OHRC'
TKFRAME_-152280_SPEC             = 'ANGLES'
TKFRAME_-152280_UNITS            = 'DEGREES'
TKFRAME_-152280_ANGLES           = ( 0.0, 90.0, -90.0 )
TKFRAME_-152280_AXES             = ( 3,   1,   2   )

\begintext

End of CH2 OHRC IK File.