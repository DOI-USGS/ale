version: '0.8.1.{build}'

image: Visual Studio 2017

platform:
  - x64

configuration:
  - Release

install:
  - git submodule update --init --recursive
  - call C:\\Miniconda36-x64\Scripts\activate.bat
  - conda config --set always_yes yes --set changeps1 no
  - conda update -q conda
  - conda install conda-build anaconda-client

before_build:
  - mkdir build
  - cd build

build_script:
  - cmake -DCMAKE_WINDOWS_EXPORT_ALL_SYMBOLS=ON -DALE_BUILD_LOAD=OFF -DALE_USE_EXTERNAL_JSON=OFF -DALE_USE_EXTERNAL_EIGEN=OFF -G "Visual Studio 15 2017 Win64" -DALE_BUILD_TESTS=OFF ..
  - cmake --build . --target ALL_BUILD --config Release

artifacts:
  - path: build\Release\ale.dll
    name: ale.dll
  - path: build\Release\ale.def
    name: ale.def

on_success:
- cd ../
- conda build -c conda-forge -q recipe/
